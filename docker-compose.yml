version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_FILE_PATH=/app/flask-server/userInfo.db
      - FLASK_ENV=development
    volumes:
      - .:/app
